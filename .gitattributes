# El proyecto se edita en Windows pero el historial guarda LF, para que un
# cambio de fin de linea no aparezca como si se hubiera reescrito el archivo.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
*.cmd text eol=crlf
